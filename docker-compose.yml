services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: checkpoint
      POSTGRES_PASSWORD: checkpoint
      POSTGRES_DB: checkpoint
    ports:
      - "5432:5432"
    volumes:
      - checkpoint_pg_data:/var/lib/postgresql/data

volumes:
  checkpoint_pg_data:
